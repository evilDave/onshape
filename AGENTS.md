## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`evilDave/onshape`). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles use matching label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Printable Thread apply

Implement the confirmed apply/builder work from `docs/architecture-review.md`. Grilling is closed.
